# AI Interview Intelligence Platform — Dependencies

# ── Web UI ────────────────────────────────────────────────────────────────────
streamlit>=1.32.0,<2.0

# ── Cloud API (transcription + fluency/content analysis only) ─────────────────
groq>=0.7.0,<1.0

# ── Local ML models ───────────────────────────────────────────────────────────
scikit-learn>=1.3.0,<2.0       # filler / voice / face sklearn pipelines
joblib>=1.3.0                  # model serialisation
tensorflow>=2.13.0,<3.0        # MobileNetV2 feature extractor (facial model)
librosa>=0.10.0,<1.0           # audio features (voice confidence model)
soundfile>=0.12.0              # librosa audio backend

# ── Video / Audio processing ──────────────────────────────────────────────────
opencv-python>=4.8.0,<5.0
mediapipe>=0.10.0,<0.11
moviepy>=1.0.3,<2.0
imageio>=2.31.0,<3.0
imageio-ffmpeg>=0.4.9,<1.0

# ── Numerics / Charts ─────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0
plotly>=5.18.0,<6.0
pillow>=10.0.0,<12.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0
